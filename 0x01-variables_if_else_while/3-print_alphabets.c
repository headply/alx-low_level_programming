#include <stdio.h>

/**
 * main - Print Small and capital a to z
 *
 * Return: always (0)
 */
int main(void)
{
	int i;

	for (i = 'a' ; i <= 'z'; i++)
		putchar(i);
	int a;

	i = a;

	for (a = 'a' ; a <= 'z'; a++)
		putchar(toupper(a));
	int putchar("\n");
	return (0);
}
