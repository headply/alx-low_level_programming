#include <stdio.h>
/**
 * main - will print from a to zin small letters
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a_to_i;
	for (a_to_z = 'a'; a_to_z <= 'z'; a_to_z++)
		putchar(a_to_z);
	putchar('\n');
	return (0);
}
