#include <stdio.h>
/**
 * main - will print from a to zin small letters
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (a_to_i = 'a'; a_to_i <= 'z'; a_to_i++)
	{
		putchar(a_to_i);
	}
	putchar('\n');
	return (0);
}
